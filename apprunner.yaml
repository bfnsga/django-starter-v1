version: 1.0
runtime: python3 
build:
  commands:
    build:        
      - pip install pipenv
      - pipenv install
run:
  runtime-version: 3.8.5
  command: pipenv run gunicorn project_config.wsgi --log-file -
  network: 
    port: 8000
  env:
    - name: ASSIGNED_DOMAIN_NAME
      value: $APPS_DOMAIN
  secrets:
    - name: SECRET_KEY
      value: - name: my-secret
      value-from: "arn:aws:secretsmanager:us-east-1:123456789012:secret:testingstackAppRunnerConstr-kJFXde2ULKbT-S7t8xR:username::"